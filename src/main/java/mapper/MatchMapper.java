package mapper;

import org.mapstruct.Mapper;

@Mapper
public interface MatchMapper {
//    MatchMapper MATCH_MAPPER = Mappers.getMapper(MatchMapper.class);
//
//    MatchesDto DTO(Matches matches);
//
//    Matches ENTITY(MatchesDto matchesDto);
}
